import React from 'react';

import Layout from '@/layout';

const Learning = (): JSX.Element => {
  return (
    <Layout>
      <div className='min-h-screen'>Learning Page</div>
    </Layout>
  );
};

export default Learning;
